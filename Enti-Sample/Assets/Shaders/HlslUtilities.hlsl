void CustomLerp_float(float A, float B, float T, out float Result)
{
    Result = lerp(A, B, T);
}

void CustomLerp3_float3_float(float3 A, float3 B, float T, out float3 Result)
{
    Result = lerp(A, B, T);
}